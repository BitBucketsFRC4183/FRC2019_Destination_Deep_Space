language: java
matrix:
  include:
  - os: linux
    dist: trusty
  - os: osx
    osx_image: xcode8

jdk:
  - oraclejdk11
  - openjdk11

before_cache:
- rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
  - "$HOME/.gradle/caches/"
  - "$HOME/.gradle/wrapper/"
script:

# Build Robot
- cd DestinationDeepSpace
- ls
- "./gradlew build"
